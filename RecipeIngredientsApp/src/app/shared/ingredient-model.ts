export class Ingredient {
  // A nice shortcut is that when you add "public" before a value in the constructor it's automatically created as a property
  constructor(public name: String, public amount: number) {}
}
